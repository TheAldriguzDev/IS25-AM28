package it.polimi.ingsw.is25am28.Model.Player;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class PlayerTest {
      Player player;

      /*
      @BeforeEach
      void init() {
            throw new Error("FIX REQUIRED (see code)");
            // TODO: Fix
            //this.player = new Player( "test", PlayerColor.BLUE );
      }

      @Test
      void add_credit_test(){
            assertEquals(3, 
                  player
                  .addCredits(1)
                  .addCredits(2)
                  .getCredits() 
            );

            System.out.println("all ok");
      }

      @Test
      void add_credit_with_lost_pieces_test(){
            assertEquals(-3, 
                  player
                  .addCredits(1)
                  .addLostPieces(4)
                  .getCredits() 
            );

            assertEquals(8, 
                  player
                  .addLostPieces(4)
                  .getLostPieces()
            );
      }

      @Test
      void check_lost_test(){
            assertEquals(
                  true, 
                  player.isEliminated()
            );

            player.setCurrentCell( new Cell(10) );

            assertEquals(
                  false, 
                  player.isEliminated()
            );
      }
       */
}
