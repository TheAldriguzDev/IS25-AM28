package it.polimi.ingsw.is25am28.Timer.TimerObserver;

public interface TimerObserver {
    /**
     * Method that will be invoked on a timer observer to do
     * something when the timer they're observing ends.
     */
    void onTimerEnd();
}
